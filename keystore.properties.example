# Copy to `keystore.properties` (git-ignored) to sign release builds.
# If this file is absent, release builds are assembled unsigned (debug still works either way).
storeFile=/absolute/path/to/mwi-release.jks
storePassword=CHANGE_ME
keyAlias=mwi
keyPassword=CHANGE_ME
