<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Host theme for the Compose UI. Kept dependency-free (framework Material parent). -->
    <style name="Theme.MWI" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/mwi_window_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
